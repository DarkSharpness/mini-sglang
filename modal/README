# Modal GPU Benchmarks

**Setup:** `uv pip install modal && modal token new`  
**HF Token (optional):** Add to `modal/.env`: `HF_TOKEN=your_token_here`  
**Run:** 
- `modal run modal/modal_app.py` - Run offline benchmark
- `modal run modal/modal_app.py --profile` - Profile prepare_for_replay() for Fused Copy optimization

**Logs:** Saved to `modal/logs/` directory with timestamps